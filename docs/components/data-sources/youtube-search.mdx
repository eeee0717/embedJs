---
title: '🔍 Youtube Search'
---

To automatically perform a youtube search and load the top youtube videos to your app, use the `YoutubeSearchLoader`.

## Install Youtube addon

```bash
npm install @cherrystudio/embedjs-loader-youtube
```

## Usage

```ts
import { RAGApplicationBuilder } from '@cherrystudio/embedjs';
import { OpenAiEmbeddings } from '@cherrystudio/embedjs-openai';
import { HNSWDb } from '@cherrystudio/embedjs-hnswlib';
import { YoutubeSearchLoader } from '@cherrystudio/embedjs-loader-youtube';

const app = await new RAGApplicationBuilder()
.setModel(SIMPLE_MODELS.OPENAI_GPT4_O)
.setEmbeddingModel(new OpenAiEmbeddings())
.setVectorDatabase(new HNSWDb())
.build();

app.addLoader(new YoutubeSearchLoader({ youtubeSearchString: '...' }))
```
