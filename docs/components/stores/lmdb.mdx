---
title: LMDB
---

You can use [LMDB](https://dbdb.io/db/lmdb) to store values locally on disk.

## Install LMDB addon

```bash
npm install @cherrystudio/embedjs-lmdb
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@cherrystudio/embedjs';
import { LmdbStore } from '@cherrystudio/embedjs-lmdb';

const app = await new RAGApplicationBuilder()
.setStore(new LmdbStore({ path: path.resolve('./store') }))
```
</CodeGroup>

<Snippet file="missing-store-tip.mdx" />
