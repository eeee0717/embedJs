---
title: Redis
---

You can use redis as a data store.

## Install Redis addon

```bash
npm install @cherrystudio/embedjs-redis
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@cherrystudio/embedjs';
import { RedisStore } from '@cherrystudio/embedjs-redis';

const app = await new RAGApplicationBuilder()
.setStore(new RedisStore({ ... }))
```
</CodeGroup>

<Snippet file="missing-store-tip.mdx" />
